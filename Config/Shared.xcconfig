// This file is attached at the project configuration level. Future target-specific
// xcconfig files must preserve the inherited, non-empty bundle identifier prefix.
// Forks and local signing setups may override the public default in Local.xcconfig.
MINDBUDGET_BUNDLE_ID_PREFIX = com.xdgf558

#include? "Local.xcconfig"
