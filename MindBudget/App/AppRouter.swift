import SwiftUI

struct AppRouter: View {
    var body: some View {
        Text("bootstrap.status.ready")
            .accessibilityIdentifier("bootstrap.status.ready")
    }
}

#Preview {
    AppRouter()
}
